### Features

- **Interactive graph screenshots**: Added screenshot thumbnails to the interactive 3D cross-modal search graph. When the "Screenshots" checkbox is enabled, nearby screenshots are fetched for each EEG-point node and displayed as billboard sprites floating above the corresponding spheres. Screenshots dim/highlight together with their parent nodes during selection. The toggle is available in the interactive mode header row alongside the existing Deduplicate and PCA cluster options.
