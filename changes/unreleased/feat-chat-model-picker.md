### Features

- **Chat model picker**: Click the model name in the chat header to switch between downloaded models without leaving the conversation. The dropdown shows all downloaded models grouped by family with size info, and seamlessly stops the current model and loads the selected one.
