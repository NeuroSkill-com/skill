### CLI

- **Bump progress bar**: `npm run bump` now shows a live progress bar during preflight checks with step counter (N/total), elapsed time, and ETA. Clippy and test steps run per-crate for granular visibility instead of one monolithic command.
