# ── VENDORED FORK of oura-api 0.1.2 ───────────────────────────────────────────
#
# Upstream (crates.io 0.1.2, the newest release) pins `reqwest 0.11`, which
# drags in the whole hyper 0.14 stack — including `h2 0.3.x`, the one branch
# still affected by RUSTSEC-2026-0258 ("h2 unbounded empty DATA frames").
# That advisory is patched in h2 >= 0.4.16 only; the 0.3 line has no fix, so
# the vulnerability cannot be resolved while anything depends on reqwest 0.11.
#
# This copy differs from crates.io ONLY in the `reqwest` version below
# (0.11 → 0.12). The crate's entire reqwest surface is
# `blocking::Client` + `get`/`bearer_auth`/`query`/`send`/`error_for_status`/
# `json` and `reqwest::Error` in its return types — all unchanged across that
# bump, so no source edits are required.
#
# Drop this patch if upstream publishes a release on reqwest 0.12+.
# Tracked at https://github.com/cluebbehusen/rust-oura-api
#
# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "oura-api"
version = "0.1.2"
exclude = [
    ".github",
    "tests",
]
description = "A client for the Oura V2 REST API"
homepage = "https://github.com/cluebbehusen/rust-oura-api"
documentation = "https://docs.rs/oura-api/latest/oura_api/"
readme = "README.md"
license = "MIT"
repository = "https://github.com/cluebbehusen/rust-oura-api"

[dependencies.paste]
version = "1.0.14"

[dependencies.reqwest]
version = "0.12"
features = [
    "json",
    "blocking",
]

[dependencies.serde]
version = "1.0.188"
features = ["derive"]

[dependencies.typed-builder]
version = "0.16.0"

[dev-dependencies.mockito]
version = "1.1.0"

[dev-dependencies.serde_json]
version = "1.0.105"
