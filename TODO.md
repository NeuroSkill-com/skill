# TODO

- [ ] when you download first model, it does not get activated by default. Fix the `use` button. And streamline first onboarding with the first model. If user click start the LLM engine, if the model is not used, ask user to go and click `use` which one they want to run.

- [x] centralize hardcoded constants into `skill-constants` with prelude module — added `prelude` module to `skill-constants` re-exporting ~60 most-used constants; moved PPG_CHANNELS, IMU_SAMPLE_RATE, signal quality thresholds (QUALITY_WINDOW, QUALITY_NO_SIGNAL_RMS, QUALITY_POOR_RMS, QUALITY_CLIP_UV, QUALITY_POOR_CLIPS, QUALITY_FAIR_RMS), blink detection params (BLINK_THRESHOLD_UV, BLINK_REFRACTORY_S, BLINK_RATE_WINDOW_S), head pose params (HEAD_POSE_ALPHA, HEAD_POSE_STILL_TAU_S, HEAD_POSE_STILL_QUIET_DPS, HEAD_POSE_STILL_ACTIVE_DPS, HEAD_POSE_NOD_THRESHOLD_DEG, HEAD_POSE_SHAKE_THRESHOLD_DEG, HEAD_POSE_GESTURE_WINDOW_S, HEAD_POSE_GESTURE_REFRACTORY_S), PPG IBI limits (PPG_IBI_MIN_S, PPG_IBI_MAX_S), SNR thresholds (SNR_LOW_DB, SNR_LOW_TICKS), session gap (SESSION_GAP_SECS), label index files (LABEL_TEXT_INDEX_FILE, LABEL_CONTEXT_INDEX_FILE, LABEL_EEG_INDEX_FILE), LLM constants (LLM_CATALOG_FILE, LLM_LOG_DIR, LLM_LOG_CAP), TTS constants (TTS_PROGRESS_EVENT, TTS_TAIL_SILENCE_SECS, KITTEN_TTS_HF_REPO, KITTEN_TTS_VOICE_DEFAULT, KITTEN_TTS_SPEED), tray (MENU_REBUILD_MIN_MS), tool calling (TOOL_CALL_START, TOOL_CALL_END, TOOL_MAX_RESULT_CHARS, TOOL_BASH_SCRIPT_THRESHOLD, TOOL_BASH_SUMMARY_HEAD/TAIL, TOOL_BASH_INLINE_THRESHOLD), DND (DND_CLIENT_ID, DND_LINUX_MODE_ID, DND_WINDOWS_MODE_ID), active window (ACTIVE_WINDOW_IDLE_THRESHOLD_SECS), and WS (WS_MAX_REQUEST_LOG) into `skill-constants`; updated 17 crate source files across 10 crates to import from the canonical source; added `skill-constants` dependency to `skill-devices`, `skill-llm`, `skill-tts`, `skill-tray`, `skill-tools`
